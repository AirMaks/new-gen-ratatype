const KeyboardErrorText = () => {
  return (
    <div className="keyboard-error">
      Change keyboard language to english and type only letters or space
    </div>
  );
};

export default KeyboardErrorText;
